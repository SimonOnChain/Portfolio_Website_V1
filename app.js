const section = document.querySelectorAll('.selection');
const sectButtons = document.querySelectorAll('.controls');
const secBtn = document.querySelectorAll('.control');
const allSelect = document.querySelectorAll('.main-content');